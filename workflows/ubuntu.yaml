name: C++ (Ubuntu Direct)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Build
      run: g++ -std=c++20 src/*.cpp -o yarc

    - name: Test
      run: ./yarc --test
